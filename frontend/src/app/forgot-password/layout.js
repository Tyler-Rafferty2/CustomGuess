export const metadata = {
  title: "Forgot Password",
  description: "Reset your CustomGuess account password.",
  robots: { index: false },
};

export default function ForgotPasswordLayout({ children }) {
  return children;
}

export const metadata = {
  title: "Browse Games",
  description: "Browse open CustomGuess games and jump into a live match.",
};

export default function LobbyLayout({ children }) {
  return children;
}

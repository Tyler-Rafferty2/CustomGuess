export const metadata = {
  title: "Sign Up",
  description: "Create a CustomGuess account to save your custom character sets and track your wins.",
};

export default function SignUpLayout({ children }) {
  return children;
}

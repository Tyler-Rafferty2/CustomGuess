export const metadata = {
  title: "Profile",
  description: "View your CustomGuess profile, game history, and custom character sets.",
  alternates: { canonical: "https://customguess.com/profile" },
};

export default function ProfileLayout({ children }) {
  return children;
}

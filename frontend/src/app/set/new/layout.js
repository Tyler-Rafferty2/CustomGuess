export const metadata = {
  title: "New Character Set",
  description: "Build a custom character set for CustomGuess. Upload photos and define the characters your friends will try to guess.",
};

export default function NewSetLayout({ children }) {
  return children;
}

export const metadata = {
  title: "Edit Character Set",
  description: "Edit your CustomGuess character set — update names, swap photos, and manage your roster.",
};

export default function EditSetLayout({ children }) {
  return children;
}
